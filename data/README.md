# Demo client files

Synthetic Class A input for the Wagtopia Demo tenant.

The browser loads the same tables from `waggy-frontend/src/b2b/fixtures.js`.
These files stay outside `waggy-frontend/` so the frontend tree does not carry CSV fixtures.

No credentials. No scientific corpus.
